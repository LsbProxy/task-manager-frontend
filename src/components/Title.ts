import styled from 'styled-components';

const Title = styled.h1`
	font-size: 2rem;
	word-break: all;
	text-align: center;
	user-select: none;
	white-space: nowrap;
	overflow: hidden;
	text-overflow: ellipsis;
`;

export default Title;
